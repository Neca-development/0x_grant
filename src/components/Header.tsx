import Button from "./Button";

function Header() {
	return (
		<header className="container flex justify-end mx-auto py-5">
			<Button text="Connect wallet"></Button>
		</header>
	);
}

export default Header;
